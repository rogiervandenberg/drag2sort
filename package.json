{
  "name": "drag2sort",
  "version": "0.0.1",
  "description": "Sort dynamically loaded articles",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rogier van den Berg",
  "license": "MIT",
  "devDependencies": {
    "gulp-connect": "^5.0.0"
  },
  "dependencies": {
    "gulp-connect": "^5.0.0"
  }
}
